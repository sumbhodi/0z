# engine.js parts — the chunked manuscript (IC pass 3, 2026-07-14)
bundle.sh concatenates *.js here (filename order) into ../engine.js + node --checks it.
EDIT THE PARTS, never engine.js. a lone part is NOT valid JS — check the assembled file.
| part | holds |
|---|---|
| 00 | header · agent defaults (incl. MC_HELPER personas) · providers · helpers |
| 10 | THE SOUL — the shared layer every agent wakes into |
| 20 | compileChart — the stable warm head |
| 30 | liveLayer · buildSystem (+ the OT line) · virtual file · channel scrubber |
| 40 | streaming (SSE) |
| 50 | history · seeding + persona migration · clock speed · engineRaw |
